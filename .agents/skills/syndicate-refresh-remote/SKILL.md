---
name: syndicate-refresh-remote
description: Synchronize a syndicate project's repositories between the local workstation and managed remote using the established fast-forward-only procedure. Use when asked to refresh or reconcile remote checkouts; do not use for independent hosts outside the estate topology.
---

# Refresh managed remote repositories

Read `.claude/commands/syndicate-refresh-remote.md` completely and execute it as the canonical
procedure, translating Claude tool names to available Codex capabilities. Resolve live repository
locations, preserve fast-forward-only behavior, never force or discard divergence, and report every
repository examined so an empty loop cannot masquerade as success.
